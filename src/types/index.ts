export interface IconProps {
  color: string;
  className?: string;
  height: number;
  width: number;
}

export enum IconType {
  contact = "contact",
  profile = "profile",
  projects = "projects",
  skills = "skills",
}
