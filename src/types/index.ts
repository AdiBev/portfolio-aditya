export interface IconProps {
  color: string;
  className?: string;
  height: number;
  width: number;
}

export enum IconType {
  notification = "notification",
  profile = "profile",
  projects = "projects",
  skills = "skills",
}
